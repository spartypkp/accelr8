import { Metadata } from "next";

export const metadata: Metadata = {
	title: "Apply to Join | Accelr8",
	description: "Apply to join the Accelr8 community and secure your spot in one of our innovative hacker houses.",
};

export default function ApplyLayout({ children }: { children: React.ReactNode; }) {
	return children;
} 